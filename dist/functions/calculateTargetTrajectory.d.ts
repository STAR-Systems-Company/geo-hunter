import { Coordinates } from '../types';
declare const calculateTargetTrajectory: (coords: Coordinates[], speed: number, height: number, interval?: number) => Coordinates[];
export default calculateTargetTrajectory;
